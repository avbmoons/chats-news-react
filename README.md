Клиент сайта с рабочими модулями «Групповые чаты», «Регистрация и авторизация пользователя» и «Новости».

Client part with working modules: “Registration and authorization” and “Group chats” and “News”.

ReactJS realization.

`npm install` needed.
`npm start` for running in development mode.
Also you can launches the test runner as `npm test`.
